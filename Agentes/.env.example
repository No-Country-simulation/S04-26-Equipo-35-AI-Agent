# ── LLM (LiteLLM: Groq / Gemini / Anthropic) ─────────────────────────────────
GROQ_API_KEY=tu_api_key_aqui
GEMINI_API_KEY=tu_gemini_key_aqui
# Ingesta masiva (~20k filas): preferir Groq para evitar 429 en Gemini free tier
LLM_SMART_PROVIDER=groq
LLM_SMART=llama-3.3-70b-versatile
LLM_BATCH_DELAY_SEC=5
LLM_CALL_DELAY_SEC=0.3
LLM_MAX_RETRIES=6
LLM_RETRY_BASE_SEC=2

# ── Paths ────────────────────────────────────────────────────────────────────
CORPUS_PATH=data/raw/corpus_mes.csv
OUTPUT_PATH=data/processed/
REPORTS_PATH=reports/

# ── Supabase (PostgreSQL en la nube) ─────────────────────────────────────────
# Obtener en: https://supabase.com/dashboard → Settings → API
SUPABASE_URL=https://tu-proyecto.supabase.co
SUPABASE_KEY=tu_service_role_key_aqui

# ── Cohere (Embeddings API) ──────────────────────────────────────────────────
# Obtener gratis en: https://dashboard.cohere.com/api-keys
COHERE_API_KEY=tu_cohere_api_key_aqui

# ── Qdrant (Base de datos vectorial — Docker local) ──────────────────────────
QDRANT_HOST=localhost
QDRANT_PORT=6333
